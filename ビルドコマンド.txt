# 順次ビルド
colcon build --executor sequential

# 依存関係のインストール
rosdep install --from-paths src -y --ignore-src

# 必要な追加パッケージのインストール
sudo apt update
sudo apt install ros-jazzy-ros2trace ros-jazzy-tracetools-analysis

# pkg 作成コマンド (例)
cd /home/kotantu-desktop/sirius_jazzy_ws/src
ros2 pkg create --build-type ament_python sirius_navigation --dependencies rclpy geometry_msgs nav_msgs sensor_msgs std_msgs